//
//  DnDTextRPGWatchApp.swift
//  DnDTextRPGWatch
//
//  Minimal watch companion — a quick status glance for the main game,
//  not a port of the full text adventure (far too small a screen for
//  that). First milestone: a standalone app that actually builds and
//  runs on watchOS; live data sync with the phone app is a follow-up.
//

import SwiftUI

@main
struct DnDTextRPGWatchApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
